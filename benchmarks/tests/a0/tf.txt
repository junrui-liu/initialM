traversal fuse {
    case Group {
    	empty;
        iterate l { do1(l); recur l; empty; }
        iterate r { empty; recur r; do1-do2(r); }
        do1(self);
        iterate l { do2(l); recur l; empty; }
        iterate r { empty; recur r; empty; }
        empty;
    }
}